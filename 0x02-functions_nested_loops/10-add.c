#include <stdio.h>
/*
 * main - entry point
 * adds two numbers
 *
 * Return: 0 success
 */

int main(void)
{
	int a;
	int b;
	int add;
	add = a + b;
	printf("Enter two numbers:\n");
	scanf("%d, %d,\n", a, b);
	printf("The sum of the nubers is: %d, \n", add);

	return (0);
}

