#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: a program that prints with pritf
 *
 * Return: Always 0 (Success)
 */
int main(void);
{
char Z;
for (Z = 'z'; Z >= 'a'; Z--)
{
putchar(Z);
}
putchar('\n');
return (0);
}
