#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: a program that prints with pritf
 *
 * Return: Always 0 (Success)
 */
int main(void);
{
char leter;
leter = z;
for (leter = 'z'; leter >= 'a'; leter--)
{
putchar('leter\n');
}
return (0);
}
