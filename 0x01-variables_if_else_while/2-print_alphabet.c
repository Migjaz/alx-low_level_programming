#include <stdlib.h>

/**
 * main - Entry - point
 * 
 * Return: Always 0 (Success)
 */

int main(void)

{
	char ltr;
	ltr = a;
	while (ltr = 'a'; ltr <= 'z'; ltr++)
	{
		putchar(ltr);
	}
	putchar("\n");

	return (0);
}
