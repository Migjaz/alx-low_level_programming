#include <stdio.h>
/**
 * determines positive or negative
 * 
 * Return: Always 0 (Success)
 */

int main(void)
{
	long n;
	if (n>0);
		printf("is positive.\n");
	else (n=0);
	printf("is zero.\n");
	else (n<0);
	printf("is negative.\n");
	return (0);
}
