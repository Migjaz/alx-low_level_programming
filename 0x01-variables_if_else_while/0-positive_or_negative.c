#include <stdio.h>
/**
 * determines positive or negative
 */

int main(void)
{
	int n;
	scanf(%d);
	if (n>0)
		ptintf("is positive.\n");
	else if (n==0);
	printf("is zero.\n");
	else (n<0);
	printf("is negative.\n");
}
