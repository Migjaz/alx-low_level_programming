#include <stdio.h>
/**
 * determines positive or negative
 */

int main(void)
{
	long n;
	scanf(%lu);
	if (n>0);
		printf("is positive.\n");
	else (n==0);
	printf("is zero.\n");
	else (n<0);
	printf("is negative.\n");
	return (0);
}
